{
  "name": "scope-agent",
  "displayName": "SCOPE – AI Coding Agent",
  "description": "An unofficial VS Code coding-agent workbench powered by the documented OpenAI Codex app-server.",
  "version": "1.0.0",
  "publisher": "PuneetGarg",
  "author": {
    "name": "Puneet Garg"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/editerzframes/scope-agent-vscode.git"
  },
  "homepage": "https://github.com/editerzframes/scope-agent-vscode#readme",
  "bugs": {
    "url": "https://github.com/editerzframes/scope-agent-vscode/issues"
  },
  "icon": "media/marketplace-icon.png",
  "galleryBanner": {
    "color": "#111827",
    "theme": "dark"
  },
  "pricing": "Free",
  "qna": "marketplace",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other",
    "Machine Learning"
  ],
  "keywords": [
    "codex",
    "agent",
    "openai",
    "ai",
    "coding",
    "coding-agent",
    "code-review",
    "inline-diff",
    "developer-tools"
  ],
  "activationEvents": [
    "onView:codexAgent.chat",
    "onCommand:codexAgent.open"
  ],
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "SCOPE can run commands and edit files, so the workspace must be trusted."
    },
    "virtualWorkspaces": {
      "supported": false
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "check": "tsc --noEmit",
    "build": "npm run check && node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "test": "node --import tsx --test test/**/*.test.ts",
    "package": "vsce package --no-dependencies"
  },
  "contributes": {
    "commands": [
      {
        "command": "codexAgent.open",
        "title": "SCOPE: Open Sidebar",
        "icon": "$(sparkle)"
      },
      {
        "command": "codexAgent.newChat",
        "title": "SCOPE: New Chat",
        "icon": "$(add)"
      },
      {
        "command": "codexAgent.showHistory",
        "title": "SCOPE: Previous Chats",
        "icon": "$(history)"
      },
      {
        "command": "codexAgent.openCommandMenu",
        "title": "SCOPE: Open Command Menu",
        "icon": "$(terminal)"
      },
      {
        "command": "codexAgent.addSelection",
        "title": "SCOPE: Add Selection to Chat"
      },
      {
        "command": "codexAgent.addFile",
        "title": "SCOPE: Add Active File to Chat"
      },
      {
        "command": "codexAgent.login",
        "title": "SCOPE: Sign In"
      },
      {
        "command": "codexAgent.logout",
        "title": "SCOPE: Sign Out"
      },
      {
        "command": "codexAgent.stop",
        "title": "SCOPE: Stop Current Turn",
        "icon": "$(debug-stop)"
      },
      {
        "command": "codexAgent.reviewChanges",
        "title": "SCOPE: Review Uncommitted Changes"
      },
      {
        "command": "codexAgent.reviewPendingChanges",
        "title": "SCOPE: Review Pending Edits Inline"
      },
      {
        "command": "codexAgent.acceptFileChange",
        "title": "SCOPE: Keep Edits in This File",
        "icon": "$(check-all)"
      },
      {
        "command": "codexAgent.rejectFileChange",
        "title": "SCOPE: Undo Edits in This File",
        "icon": "$(discard)"
      },
      {
        "command": "codexAgent.acceptAllChanges",
        "title": "SCOPE: Keep All Pending Edits"
      },
      {
        "command": "codexAgent.showStatus",
        "title": "SCOPE: Show Account and Usage"
      },
      {
        "command": "codexAgent.openSettings",
        "title": "SCOPE: Open Settings"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "codexAgent",
          "title": "SCOPE",
          "icon": "media/codex-agent.svg"
        }
      ]
    },
    "views": {
      "codexAgent": [
        {
          "type": "webview",
          "id": "codexAgent.chat",
          "name": "Agent"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "codexAgent.showHistory",
          "when": "view == codexAgent.chat",
          "group": "navigation@1"
        },
        {
          "command": "codexAgent.newChat",
          "when": "view == codexAgent.chat",
          "group": "navigation@2"
        },
        {
          "command": "codexAgent.openCommandMenu",
          "when": "view == codexAgent.chat",
          "group": "navigation@3"
        }
      ],
      "editor/context": [
        {
          "command": "codexAgent.acceptFileChange",
          "when": "codexAgent.activeFileHasPendingChanges",
          "group": "navigation@1"
        },
        {
          "command": "codexAgent.rejectFileChange",
          "when": "codexAgent.activeFileCanReject",
          "group": "navigation@2"
        },
        {
          "command": "codexAgent.addSelection",
          "when": "editorHasSelection",
          "group": "navigation@50"
        },
        {
          "command": "codexAgent.addFile",
          "group": "navigation@51"
        }
      ],
      "editor/title": [
        {
          "command": "codexAgent.acceptFileChange",
          "when": "codexAgent.activeFileHasPendingChanges",
          "group": "navigation@1"
        },
        {
          "command": "codexAgent.rejectFileChange",
          "when": "codexAgent.activeFileCanReject",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": {
      "title": "SCOPE",
      "properties": {
        "codexAgent.cliExecutable": {
          "type": "string",
          "default": "",
          "description": "Optional absolute path to the Codex CLI executable. Leave empty to auto-detect it."
        },
        "codexAgent.model": {
          "type": "string",
          "default": "",
          "description": "Default Codex model. Leave empty to use the server-selected default."
        },
        "codexAgent.reasoningEffort": {
          "type": "string",
          "default": "",
          "description": "Optional reasoning effort sent with each turn (for example low, medium, high, or xhigh)."
        },
        "codexAgent.sandbox": {
          "type": "string",
          "enum": [
            "read-only",
            "workspace-write",
            "danger-full-access"
          ],
          "enumDescriptions": [
            "Allow inspection only.",
            "Allow edits within the active workspace.",
            "Disable the filesystem sandbox. Use only in controlled environments."
          ],
          "default": "workspace-write",
          "description": "Filesystem sandbox applied to new Codex threads."
        },
        "codexAgent.approvalPolicy": {
          "type": "string",
          "enum": [
            "on-request",
            "untrusted",
            "never"
          ],
          "enumDescriptions": [
            "Ask when Codex needs elevated access.",
            "Require approval for commands not covered by trusted rules.",
            "Never display approval prompts."
          ],
          "default": "on-request",
          "description": "Approval policy applied to new Codex threads."
        },
        "codexAgent.inlineReview.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Highlight Codex changes in the existing editor, show compact per-hunk Undo/Keep actions above each changed block, and reveal the before/after diff on hover."
        },
        "codexAgent.selectionAction.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show an Add to Chat action above selected code in the active editor."
        }
      }
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/vscode": "1.100.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
